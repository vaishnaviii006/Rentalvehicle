# Production API base URL
# Set this to your deployed backend URL on Vercel.
# For monorepo deployments (frontend + backend on same Vercel project), leave empty.
# For split deployments (separate Vercel projects), set the backend URL.
VITE_API_BASE_URL=
